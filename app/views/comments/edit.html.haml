%h1= "Edit Comment"

- form_with (model: [@article, @article.comments.joins(params[:id])], local: true) do |form|
    %div 
        = form.label :commenter
        = form.text_field :commenter

    %div 
        = form.label :body
        = form.text_area :body

    %div
        = form.submit